# ── Supabase Local Development Configuration ─────────────────────────────────
# This file configures the Supabase CLI for local development.
# Run `supabase init` to generate the full config, then `supabase link` to link
# to your remote project.
#
# Your remote project ref: lismrowveczddsinhexw
# Dashboard: https://supabase.com/dashboard/project/lismrowveczddsinhexw
#
# To initialize:
#   1. npx supabase login
#   2. npx supabase link --project-ref lismrowveczddsinhexw
#   3. npx supabase db push    (apply schema to remote)
#   4. npx supabase gen types typescript --linked > src/types/supabase.ts
#
# Note: supabase/config.toml is auto-generated by `supabase init`.
# This file documents the remote project connection only.

[project]
id = "lismrowveczddsinhexw"